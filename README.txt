upnpower

A tool for enumerating and getting all information from a UPNP enabled router over the network.

Returns information.
Returns Services
Returns Service.Actions
Returns Service.Actions.Arguments

If Service.Action takes no arguments then the Service.Action is executed as a function to return data.
